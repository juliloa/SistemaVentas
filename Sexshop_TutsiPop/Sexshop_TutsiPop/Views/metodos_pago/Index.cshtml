@model IEnumerable<Sexshop_TutsiPop.Models.metodos_pago>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.metodo_pago)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.activo)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.metodo_pago)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.activo)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.id_metodo">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.id_metodo">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.id_metodo">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
